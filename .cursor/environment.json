{
  "name": "Robotics Inventory",
  "install": "npm ci --prefix backend\nnpm ci --prefix frontend",
  "terminals": [
    {
      "name": "backend",
      "command": "npm run dev --prefix backend",
      "description": "Express API with nodemon at http://localhost:3001. GET /api/health for readiness. JSON database lives in backend/data/data.json."
    },
    {
      "name": "frontend",
      "command": "npm run dev --prefix frontend -- --host 0.0.0.0 --port 5173",
      "description": "Vite React app at http://localhost:5173. Proxies /api and /uploads to the backend on port 3001."
    }
  ],
  "ports": [
    { "name": "frontend", "port": 5173 },
    { "name": "backend", "port": 3001 }
  ]
}
